name ideas:
Dinner Is Easy Tonight
Long Winded Recipes

user class has:
#id
#username
#email
#password (hashed)
#avatar
#createdAt
#role/type of user

ingredient class has:
#id
#name
#url/affiliate link
#image

recipe class has:
#ingredients: [
  { name: "flour", amount: "2", unit: "cups" }
]
#name
#cooktime
#preptime
#directions
#yield
#authorId
#url
#image

request mappings for:
/api

AUTH:
  POST /auth/login
  POST /auth/register

RECIPES:
  GET    /recipes
  GET    /recipes/{id}
  POST   /recipes
  PUT    /recipes/{id}
  DELETE /recipes/{id}

USERS:
  GET    /users
  GET    /user/me
  GET    /users/{id}
  PUT    /users/{id}
  DELETE /users/{id}

INGREDIENTS:
  GET    /ingredients
  GET    /ingredients/{id}
  POST   /ingredients
  PUT    /ingredients/{id}
  DELETE /ingredients/{id}





